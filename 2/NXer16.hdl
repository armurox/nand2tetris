/**
 * 16-bit NXer:
 * out = !in if in = 1 else out = in
 */
CHIP NXer16 {
    IN in[16], sel;
    OUT out[16];

    PARTS:
    DMux16(in=in, sel=sel, a=dmux1, b=dmux2);
    Not16(in=dmux2, out=not1);
    Mux16(a=dmux1 , b=not1, sel=sel , out=out);
}